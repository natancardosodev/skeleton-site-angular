export const nenhumaUF = [];

export const todasUFs = [];

export const urlPortalHttp = `http://${window.location.host}`;

export const env = window.location.host.split('.')[0];

export const producao = 'www';

export const uf = window.location.href.split('.')[2] || 'pb';
